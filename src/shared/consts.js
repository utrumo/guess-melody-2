const QuestionType = {
  ARTIST: `artist`,
  GENRE: `genre`
};

const ActionType = {
  INCREMENT_STEP: `INCREMENT_STEP`,
  INCREMENT_MISTAKES: `INCREMENT_MISTAKES`,
  RESET: `RESET`,
  TICK_TIMER: `TICK_TIMER`
};

export {
  QuestionType,
  ActionType
};
